name: CI

on:
  push:
    branches: [develop, main, stoop-test, stoop-production]
  pull_request:

# Cancel superseded runs on the same ref to save minutes.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Typecheck, build & test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Install
        run: npm ci
      # `build` runs `tsc && vite build`, so this is the typecheck + bundle gate.
      - name: Typecheck & build
        run: npm run build
      # White-label gate: the hawk brand must keep building (skips tsc — same
      # sources were just typechecked above).
      - name: Build white-label brand (hawk)
        run: npx vite build
        working-directory: apps/web
        env:
          VITE_BRAND: hawk
      - name: Unit tests
        run: npm test
